#include <stdio.h>
int main() {
	const int max = 10;
	int i;
	double hit =21.5, key, jobs =0.0;
	for (i=1; i<= max; ++i) {
	if (hit<max>hit){
	goto jump;
	}
	jobs += hit;
	}
jump:
	key = jobs/(i*2);
	printf("Jobs = %.2f\n",jobs);
	printf("Key = %.1f\n",key);
	return 0;
	}
